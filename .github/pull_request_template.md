## What this changes

<!-- One or two sentences. For a style: name, category, and what makes its construction distinct. -->

## Checklist

- [ ] `npm test`, `npm run lint` and `npm run build` pass
- [ ] Style packages: folder under `styles/community/<slug>/` with `style.json`, `metadata.json`, `preview.svg` (`npm run registry:previews`) and an entry in `index.json`; `npm run registry:check` passes
- [ ] Engine changes: `GENERATOR_VERSION` bumped, `npm run engine:data` re-run, `npm run engine:test` passes
- [ ] Screenshots attached for visual changes
